# 🃏 WebSocket Game Server

A simple Node.js WebSocket server that manages players, turns, and game actions.
It serves the frontend (`index.html`) and handles WebSocket communication on the same port.

## 📦 Installation

Make sure you have Node.js installed.

Install dependencies:

```bash
npm install
```

Start server:

```bash
npm run server
```